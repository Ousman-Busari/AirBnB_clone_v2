<!DOCTYPE html>
<html lang="en">
  <head>
    <title>HBNB</title>
  </head>
  <body>
    <h1>States</h1>
    <ul>
      {% for state in list_states.values() %}
      <li>{{"{}: <b>{}</b>".format(state.id, statte.name}}</li>
      {% endfor %}
    </ul>
  </body>
</html>
